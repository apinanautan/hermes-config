# Context / Token Bloat Fix

- Keep secret values out of config; use env references.
- Do not inject root docs and memory sources more than once.
- Prune tool logs and old history before compaction.
- Validate with a token/char breakdown before upload.
- Focus on the smallest set of files that explain the bloat.
